---
import { PROJECTS } from '@/utils/constants/projects'
import GitHub from './icons/GitHub.astro'
import Link from './icons/Link.astro'
import LinkButton from './LinkButton.astro'
---

<div class='flex flex-col gap-y-16'>
  {
    PROJECTS.map(({ image, title, description, tags, link, github }) => (
      <article class='flex flex-col space-x-0 space-y-8 group md:flex-row md:space-x-8 md:space-y-0'>
        <div class='w-full md:w-1/2'>
          <a href={link} target='_blank'>
            <div class='relative flex flex-col items-center col-span-6 row-span-5 gap-8 transition duration-500 ease-in-out transform shadow-xl overflow-clip rounded-xl sm:rounded-xl md:group-hover:-translate-y-1 md:group-hover:shadow-2xl lg:border lg:border-gray-800 lg:hover:border-gray-700 lg:hover:bg-gray-800/50'>
              <img
                alt={title}
                class='object-cover object-top transition duration-500 scale-1 md:scale-110 md:group-hover:scale-105'
                loading='lazy'
                src={image.src}
              />
            </div>
          </a>
        </div>

        <div class='w-full md:w-1/2 md:max-w-lg'>
          <h3 class='text-2xl font-bold '>{title}</h3>
          <div class='flex flex-wrap mt-2'>
            <ul class='flex flex-row flex-wrap mb-2 gap-2'>
              {tags.map((tag) => (
                <li>
                  <span
                    class={`flex leading-0 gap-x-2 rounded-full text-xs ${tag.class} py-1.5 px-2 `}
                  >
                    <tag.icon class='size-4' />
                    {tag.name}
                  </span>
                </li>
              ))}
            </ul>

            <div class='mt-2 text-gray-700 dark:text-gray-400'>
              {description}
            </div>
            <footer class='flex items-end justify-start mt-4 gap-x-4'>
              {github && (
                <LinkButton href={github}>
                  <GitHub class='size-5' />
                  Code
                </LinkButton>
              )}
              {link && (
                <LinkButton href={link}>
                  <Link class='size-4' />
                  Preview
                </LinkButton>
              )}
            </footer>
          </div>
        </div>
      </article>
    ))
  }
</div>
